## Cloud Worklfow Secrets

The secrets are defined here, but must be pre-populated before deployment.

In Cloud Local env, they should be modified appropriately and applied without committing.

In Dev/Staging/Production environment, they should be populated from Vault according to the definition/structure defined here.
